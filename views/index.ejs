<h2>Recently Added</h2>
<div>
    <% books.forEach(book => { %>
      <img height="150" width="100" src="<%= book.coverImagePath %>" alt="image">
      <div><%= book.title %></div>
      <div><%= book.publishDate %></div>
    <% }) %>
  </div>
  